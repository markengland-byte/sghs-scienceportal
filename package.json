{
  "name": "sghs-portal",
  "version": "1.0.0",
  "private": true,
  "description": "SGHS Science Portal — static site + Vercel serverless functions",
  "scripts": {
    "test": "playwright test",
    "test:install": "playwright install chromium --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
